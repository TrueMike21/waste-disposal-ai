version: '3.9'
services:
  web:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ../model:/app/model
    environment:
      - FLASK_ENV=production